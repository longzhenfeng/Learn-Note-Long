{
  "name": "learn-note-long",
  "private": true,
  "scripts": {
    "docs:dev": "pnpm --filter docs dev",
    "docs:build": "pnpm --filter docs build",
    "docs:preview": "pnpm --filter docs preview",
    "express:dev": "pnpm --filter express-app dev",
    "express:start": "pnpm --filter express-app start",
    "vue:dev": "pnpm --filter vue-app dev",
    "db:start": "pnpm --filter express-app run db:start",
    "db:view": "pnpm --filter express-app run db:view"
  },
  "engines": {
    "node": ">=22.0.0"
  }
}
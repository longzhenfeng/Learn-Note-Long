{
  "name": "vue-app",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "engines": {
    "node": ">=22.0.0"
  },
  "scripts": {
    "dev": "vite --port 3001 --host",
    "build": "vue-tsc -b && vite build",
    "preview": "vite preview",
    "cap:sync": "cap sync",
    "cap:sync:ios": "cap sync ios",
    "cap:sync:android": "cap sync android",
    "cap:open:ios": "cap open ios",
    "cap:open:android": "cap open android",
    "cap:build:ios": "pnpm build && cap sync ios && cap open ios",
    "cap:build:android": "pnpm build && cap sync android && cap open android"
  },
  "dependencies": {
    "@capacitor/android": "^8.0.0",
    "@capacitor/cli": "^8.0.0",
    "@capacitor/core": "^8.0.0",
    "@capacitor/ios": "^8.0.0",
    "@capacitor/status-bar": "^8.0.0",
    "axios": "^1.13.2",
    "vue": "^3.5.24"
  },
  "devDependencies": {
    "@types/node": "^22.19.3",
    "@vitejs/plugin-vue": "^5.2.4",
    "@vue/tsconfig": "^0.8.1",
    "sass": "^1.97.1",
    "typescript": "~5.9.3",
    "vite": "^5.4.21",
    "vue-tsc": "^3.1.4"
  }
}
